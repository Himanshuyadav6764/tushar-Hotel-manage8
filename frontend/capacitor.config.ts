import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bireena.atithi',
  appName: 'Bireena Atithi',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
